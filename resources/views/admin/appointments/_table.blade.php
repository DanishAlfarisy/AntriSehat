<div class="bg-white rounded-2xl shadow overflow-x-auto"><table class="w-full text-sm"><thead class="bg-slate-100"><tr><th class="p-3 text-left">Tanggal</th><th class="p-3 text-left">Pasien</th><th class="p-3 text-left">Dokter</th><th class="p-3 text-left">No</th><th class="p-3 text-left">Status</th><th class="p-3 text-left">Aksi</th></tr></thead><tbody>
@forelse($appointments as $appointment)<tr class="border-t"><td class="p-3">{{ $appointment->appointment_date }}</td><td class="p-3">{{ $appointment->patient->name }}</td><td class="p-3">{{ $appointment->doctorSchedule->doctor->name }}</td><td class="p-3 font-bold">{{ $appointment->queue_number }}</td><td class="p-3"><x-status-badge :status="$appointment->status" /></td><td class="p-3"><div class="flex flex-wrap gap-2">@if($appointment->status==='pending')<form method="POST" action="{{ route('admin.appointments.confirm',$appointment) }}">@csrf<button class="rounded bg-blue-600 px-2 py-1 text-white">Confirm</button></form>@endif @if(in_array($appointment->status,['pending','confirmed']))<form method="POST" action="{{ route('admin.appointments.cancel',$appointment) }}">@csrf<button class="rounded bg-red-500 px-2 py-1 text-white">Cancel</button></form>@endif @if($appointment->status==='confirmed')<form method="POST" action="{{ route('admin.appointments.complete',$appointment) }}">@csrf<button class="rounded bg-emerald-600 px-2 py-1 text-white">Complete</button></form>@endif</div></td></tr>@empty<tr><td colspan="6" class="p-4 text-center text-slate-500">Belum ada appointment.</td></tr>@endforelse
</tbody></table></div>
